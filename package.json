{
  "name": "yorespondo-agente",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "probar": "node --env-file-if-exists=.env.local scripts/probar.ts",
    "typecheck": "tsc --noEmit",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": ">=0.110.0",
    "@supabase/supabase-js": "^2.45.4",
    "@vercel/functions": ">=1.5.0"
  },
  "devDependencies": {
    "@types/node": ">=22.7.4",
    "typescript": "^5.6.2"
  }
}
